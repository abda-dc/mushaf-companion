# Warsh ʿan Nāfiʿ — KFGQPC candidate intake

Status: authoritative-source candidate only. Not runtime content.

This directory records the known identity of the King Fahd Glorious Qur'an Printing Complex Warsh developer package while the official KFGQPC endpoints are unreachable from the current development environment.

The actual package has not been received or inspected. Provider-published MD5 and SHA-1 values below are source metadata only; they have not been locally verified against artifact bytes.

This candidate must not be imported by application runtime code, copied to public output, registered as a Quran reading, registered as a Quran page edition, exposed by the Settings reading selector, or used to enable Warsh audio.

Before any runtime activation, the exact provider artifact must be acquired from an authoritative KFGQPC endpoint and pass all gates in activation-gates.json, including provider-published hash matching, a new locally calculated SHA-256, archive inventory review, package/readme rights review, ayah-numbering audit, page-boundary audit, line-layout audit, font audit, and separate audio compatibility approval.
